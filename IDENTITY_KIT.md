# Visual Identity Kit — Tokollo Mokalodise Portfolio

## Core Mood Sentence
A disciplined, evidence-first technical workspace that frames code execution, metric audits, and system contracts without decorative polish or visual filler.

## Color Tokens & Palette
- Canvas Background (`bg`): `#F8FAFC` (Slate 50 — Near-white canvas)
- Card Surface (`card`): `#FFFFFF` (Pure White)
- Primary Text (`text`): `#0F172A` (Slate 900 — High-contrast text)
- Muted Text (`muted`): `#64748B` (Slate 500 — Secondary text)
- Primary Accent (`accent`): `#0284C7` (Sky 600 — Action color)
- Structural Borders (`border`): `#E2E8F0` (Slate 200 — Divider lines)
- Terminal/Code Block (`code-bg`): `#0F172A` (Slate 900 — Dark code/stdout container)

## Typography Hierarchy
- Primary Sans-Serif: `Inter`, system-ui, sans-serif
- Technical Monospace: `JetBrains Mono`, `Fira Code`, monospace

## Favicon & Logo Mark
- Logo/Mark: Plain-text monospaced wordmark `[TM]` in `#0284C7` on `#F8FAFC`.
